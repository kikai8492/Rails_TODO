<h1><%= @user.name%>のページ</h1>
<h2>メールアドレス:<%= @user.email%></h2>

<h4>以下、タスク一覧</h4>
<% @user.tasks.each do |task|%>
  <p>タスク名:<%= task.not_started_yet%></p>
<%end%>
<%= link_to "プロフィールの編集", edit_user_path%>

<%= link_to "戻る", tasks_path%>